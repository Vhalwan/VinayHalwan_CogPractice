services:
  - type: web
    name: bankapi
    env: java
    branch: banking-rest-api
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/bankapi-0.0.1-SNAPSHOT.jar
    envVars:
      - key: MONGO_URI
        sync: false
